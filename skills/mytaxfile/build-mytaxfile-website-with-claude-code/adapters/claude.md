# Claude Adapter — build-mytaxfile-website-with-claude-code

This adapter has no divergence from the canonical `SKILL.md` in this folder — follow it directly.

Claude-specific notes only:

- This project also falls under Seung's personal `design-stable-web-frames` and `apply-world-class-expert-standard` skills, which are not part of this repo and are not duplicated here. Load them from Claude's skill set when doing frame/content work; the canonical Skill file assumes their rules are in effect.
- When Google Drive access is needed, scope all searches/reads to the "2.1 myTaxfile" folder only.
